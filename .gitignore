# Dependencies
node_modules

# Nuxt build output
.nuxt
.output
dist

# Environment — never commit secrets. Only .env.example is tracked.
.env
.env.*
!.env.example

# Logs and OS noise
*.log
.DS_Store
Thumbs.db

# Deploy and tooling state
.vercel
coverage

# Playwright artifacts (screenshots, traces, HTML report)
test-results
playwright-report
blob-report
.playwright

# Firecrawl scratch output
.firecrawl

# Disposable intermediates (screenshots, scratch exports)
.tmp

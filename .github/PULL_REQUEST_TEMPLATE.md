## What this changes

<!-- One or two sentences. What is different for a visitor to the site? -->

## Why

<!-- The problem, the correction, or the issue this closes. -->

## Checks

- [ ] `npm run verify` passes
- [ ] `npm run eval:chat` passes (only if `shared/chat/`, `api/chat.ts`, or `content/` changed)
- [ ] Any new or changed claim carries an evidence label that matches its source
- [ ] No secrets, credentials, or unapproved personal data added

## Owner decisions

<!-- Anything that needs a human call before this ships: production deploys,
     env vars, public claims, legal or contact details. Write "none" if none. -->
